#compdef build

local -a targets=(
  '--help:Show usage info'
  '--list:Print target names'
  'all:Build every target'
)
local t
for t in ${(f)"$(build --list)"}; do
  [[ $t == all ]] || targets+=("$t:Build $t from $REPOS_DIR/$t")
done
_describe 'build targets' targets
