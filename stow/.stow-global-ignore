\.DS_Store

# Root-level package metadata (docs, not config); anchored so same-named files
# nested deeper (e.g. claude-code/.claude/CLAUDE.md) still deploy
^/AGENTS\.md$
^/CLAUDE\.md$
^/README\.md$
^/assets$
