[metadata]
name = "Files"
description = "A channel to select files and directories"
requirements = ["fd", "bat"]

[source]
command = "fd -t f --color=never -0 --exec eza --icons=always --color=always"
ansi = true

# [preview]
# command = "bat -n --color=always {substring:1..}"
# env = { BAT_THEME = "Catppuccin Mocha" }

[ui]
preview_panel = { "size" = 50, "scrollbar" = false }

[keybindings]
shortcut = "f1"
