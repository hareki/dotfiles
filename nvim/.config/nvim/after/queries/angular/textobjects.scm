; The angular grammar is a tree-sitter-html fork with identical node names, so
; inheriting resolves the full html textobjects query, including the @tag.*
; extensions in after/queries/html/textobjects.scm.
; inherits: html
