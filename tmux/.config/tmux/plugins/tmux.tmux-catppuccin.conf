set -g @catppuccin_flavor 'mocha'
set -g @catppuccin_status_background '#{@thm_bg}'
set -g @catppuccin_window_status_style 'rounded'

set -g @catppuccin_window_text " #[fg=#{@thm_subtext_0}]#{window_name}"
set -g @catppuccin_window_number "#{window_index}"
set -g @catppuccin_window_text_color "#{@thm_surface_0}"
set -g @catppuccin_window_number_color "#{@thm_subtext_0}"

set -g @catppuccin_window_current_text " #[fg=#{@thm_green}]#{window_name}"
set -g @catppuccin_window_current_number "#{window_index}"
set -g @catppuccin_window_current_text_color "#{@thm_surface_0}"
set -g @catppuccin_window_current_number_color "#{@thm_green}"

set -g @catppuccin_user_color "#{@thm_blue}"
set -g @catppuccin_status_user_text_fg "#{@thm_blue}"

set -g @catppuccin_application_color "#{@thm_lavender}"
set -g @catppuccin_status_application_text_fg "#{@thm_lavender}"

set -g @catppuccin_host_color "#{@thm_mauve}"
set -g @catppuccin_status_host_text_fg "#{@thm_mauve}"

set -g @catppuccin_session_color "#{?client_prefix,#{E:@thm_red},#{E:@thm_blue}}"
set -g @catppuccin_status_session_text_fg "#{?client_prefix,#{E:@thm_red},#{E:@thm_blue}}"

set -g @catppuccin_session_icon " " 
set -g @catppuccin_application_icon "󰲋 "
set -g @catppuccin_user_icon " " 
set -g @catppuccin_host_icon "󰒋 " 


set -g @catppuccin_status_connect_separator "no" 
set -g @catppuccin_status_left_separator ""
set -g @catppuccin_status_middle_separator ""
set -g @catppuccin_status_right_separator ""


# set -g @catppuccin_pane_active_border_style "##{?pane_in_mode,fg=#{@thm_blue},##{?pane_synchronized,fg=#{@thm_mauve},fg=#{@thm_blue}}}"
set -g @catppuccin_pane_active_border_style "##{?pane_in_mode,fg=#{@thm_overlay_0},##{?pane_synchronized,fg=#{@thm_mauve},fg=#{@thm_overlay_0}}}"

set -g status-left "#{E:@catppuccin_status_session} "
set -g status-right "#{E:@catppuccin_status_application} "
set -ag status-right "#{E:@catppuccin_status_user} "
set -ag status-right "#{E:@catppuccin_status_host}"
